apply plugin: 'groovy'

sourceSets {
    main {
        groovy {
            srcDirs = ['src', 'vars']
        }
    }
}

repositories {
    jcenter()
}

dependencies {
    implementation 'org.codehaus.groovy:groovy-all:2.5.11'
}
